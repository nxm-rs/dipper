target/
**/*.rs.bk
*.tar.gz
.env

# Nix
.direnv/

# Editors
.zed/

# Agent / local worktrees and session state (never tracked)
# Track shared Claude config (hooks + team settings); ignore personal + session state.
.claude/*
!.claude/settings.json
!.claude/hooks/
.claude/settings.local.json
.claude/worktrees/
.worktrees/

# Workflow scratch directories
/.dipper-work-*
/.dipper-handoff/
